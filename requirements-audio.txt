# Audio generation environment (tts-models conda env)
# Install: pip install -r requirements-audio.txt
# Python 3.11 recommended. PyTorch wheels are platform-specific; adjust torch/torchaudio if needed.
#
# Covers the 17 smoke-tested TTS models plus optional server runners
# (Fish Speech, CosyVoice2, IndexTTS-2, Stable Audio Open).

# --- API server ---
fastapi==0.136.3
uvicorn==0.48.0
python-dotenv==1.2.2
pydantic==2.9.2
python-multipart==0.0.30

# --- Core ML stack ---
torch==2.12.0
torchaudio==2.11.0
torchvision==0.27.0
transformers==5.9.0
accelerate==1.13.0
safetensors==0.5.3
huggingface_hub==1.17.0
tokenizers==0.22.2
sentencepiece==0.2.0
protobuf==4.25.9
diffusers==0.29.0

# --- Audio I/O and signal processing ---
numpy==1.26.4
scipy==1.17.1
soundfile==0.13.1
librosa==0.11.0
audioread==3.1.0
soxr==1.1.0
pydub==0.25.1

# --- Text / phoneme frontends ---
phonemizer==3.3.0
phonemizer-fork==3.3.2
espeakng-loader==0.2.4
unidecode==1.4.0
num2words==0.5.14
inflect==7.5.0
regex==2026.5.9

# --- Hugging Face datasets (used by several runners) ---
datasets==2.18.0
pyarrow==24.0.0

# --- Config / utilities ---
PyYAML==6.0.3
einops==0.8.2
tqdm==4.67.3
requests==2.34.2
pillow==9.5.0

# --- Smoke-tested TTS models (17/17 passing) ---

# Kokoro-82M (PyTorch)
kokoro==0.9.4
misaki==0.9.4

# Kokoro-82M ONNX
onnxruntime==1.26.0
onnx==1.21.0

# MMS-TTS, SpeechT5, Bark (transformers-only; no extra packages)

# OuteTTS 0.2-500M / 0.3-1B
outetts==0.4.4
llama_cpp_python==0.3.9

# MeloTTS English
melo==1.1.2
melotts @ git+https://github.com/myshell-ai/MeloTTS.git@209145371cff8fc3bd60d7be902ea69cbdb7965a

# ESPnet VITS (LJSpeech)
espnet==202511
espnet-model-zoo==0.1.7
espnet-tts-frontend==0.0.3
kaldiio==2.18.1
typeguard==4.5.2

# Chatterbox TTS
chatterbox-tts==0.1.7
resemble-perth==1.0.1
conformer==0.3.2
s3tokenizer==0.3.0
vocos==0.1.0
descript-audio-codec==1.0.0

# F5-TTS and E2-TTS
f5-tts==1.1.20
ema-pytorch==0.7.9
x-transformers==2.19.7
torchdiffeq==0.2.5

# KittenTTS Nano
kittentts==0.1.3

# --- Optional: additional audio_server runners (not in smoke test) ---

# Fish Speech v1.5
fish-speech==0.1.0
snac==1.2.1

# CosyVoice2 (requires local model checkout with third_party/Matcha-TTS)
cosyvoice==0.0.5
modelscope==1.17.1
hydra-core==1.3.2
omegaconf==2.3.0
opencc-python-reimplemented==0.1.7

# IndexTTS-2 (install index-tts runtime separately from model repo)
# pip install index-tts  # or clone IndexTeam/IndexTTS and pip install -e .

# Stable Audio Open 1.0 (text-to-audio; uses diffusers above)
